Base Kit (prefix: "Base -")

Includes site settings, global header/footer placeholders, base archive/404, and core page skeletons.
Export via Elementor → Tools → Export Kit (Include Site Settings + Templates + Content).
Place the zip here as `base_kit_v1.zip`.



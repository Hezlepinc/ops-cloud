Check drift:

- Compare /templates/template.manifest.json "version" with WP option hezlep_kit_version via /ai/wordpress/:brand.
- If remote < local: propose PR or orchestrator import call (staging first).



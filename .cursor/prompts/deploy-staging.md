Deploy staging:

- If changes in /infra/wordpress/theme/** → trigger Deploy Theme (staging) workflow.
- If changes in /templates/** → trigger Import Templates (staging) workflow.
- After run, fetch /ai/status and show kit version + theme hash.



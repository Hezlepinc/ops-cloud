{
  "name": "ops-cloud",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.11.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier -w .",
    "format:check": "prettier -c .",
    "build:themes": "echo 'No theme build yet'"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.1.1"
  }
}
